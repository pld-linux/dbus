# D-BUS configuration
# start D-BUS session daemon while login to system
SESSION_BUS_PROFILE_D="yes"

# Don't restart messagebus service on package
# upgrade - kills all running dbus services
RPM_SKIP_AUTO_RESTART=yes
